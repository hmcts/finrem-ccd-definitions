import { test } from '../fixtures/fixtures';

test.skip(
    'Consented Tab Verification',
    { tag: ["@preview"] },
    async (
      { 
      },
    ) => {
        //TODO: Migrate over TabVerification checks seen in codecept `consented_test.js` preview tests to playwright. 
    }
);

test.skip(
    'Contested Tab Verification',
    { tag: ["@preview"] },
    async (
      { 
      },
    ) => {
        //TODO: Migrate over TabVerification checks seen in codecept `contested_test.js` preview tests to playwright. 
    }
);
